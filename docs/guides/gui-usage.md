# GUI usage

Run:

```bash
python -m make_markdown_library gui
```

Choose the source folder, output file, converter mode, Markdown policy, and index format. The GUI also includes LiteParse install help and a build summary.
